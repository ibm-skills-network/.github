name: Detect Secrets

on:
  pull_request:
  merge_group:

concurrency:
  group: detect-secrets-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  issues: write # Adding a label the repo does not have yet creates it, and making labels needs Issues.

jobs:
  detect-secrets:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      REPO: ${{ github.repository }}
      PR: ${{ github.event.pull_request.number }}
      # Finds the comment we left last time so we edit that one instead of adding another.
      MARKER: '<!-- detect-secrets-check -->'
      # 'false' lets a repo with no baseline pass with a notice, so this check can run
      # everywhere while repos are still being onboarded. A repo that has a baseline is
      # enforced either way. 'true' requires every repo to have one.
      REQUIRE_BASELINE: 'false'
      # master, not a tag. The newest tag, v0.13.1, is from March 2020 and sits 675 commits
      # behind. It declares VERSION 0.13.1 where master declares 0.13.1+ibm.64.dss, and that
      # ibm.N counts IBM's own builds, so a tag gets you the pre-IBM code with none of the IBM
      # detectors. IBM's install instructions say @master too.
      #
      # Tracking master means a detector added upstream starts applying here the day it lands,
      # with no change to this file. A repo that passes today can block tomorrow. That is the
      # trade being made on purpose: finding a secret late is worse than a surprise red check.
      #
      # To stop that while you deal with something, pin a commit here instead:
      #
      #   gh api repos/ibm/detect-secrets/commits/master --jq .sha
      #
      # and put it back to master once the reason for pinning is gone.
      DETECT_SECRETS_REF: master

    steps:
      - uses: actions/checkout@v4

      - name: Look for a baseline
        id: baseline
        run: |
          set -euo pipefail
          if [ -f .secrets.baseline ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Handle a repository with no baseline
        if: steps.baseline.outputs.present == 'false'
        run: |
          set -euo pipefail
          if [ "$REQUIRE_BASELINE" = "true" ]; then
            echo "::error::This repository has no .secrets.baseline, and every repository is now required to have one."
            exit 1
          fi
          echo "::notice::No .secrets.baseline here, so secret scanning is not enforced in this repository yet."

      - uses: actions/setup-python@v5
        if: steps.baseline.outputs.present == 'true'
        with:
          python-version: '3.12'

      # An install failure fails the check. A pull request that was never scanned must not
      # report success.
      - name: Install IBM detect-secrets
        id: install
        if: steps.baseline.outputs.present == 'true'
        run: |
          set -euo pipefail
          pip install --quiet --upgrade \
            "git+https://github.com/ibm/detect-secrets.git@${DETECT_SECRETS_REF}#egg=detect-secrets"
          detect-secrets --version

      - name: Scan and enforce
        id: scan
        if: steps.baseline.outputs.present == 'true' && steps.install.outcome == 'success'
        run: |
          set -uo pipefail

          VERSION=$(python3 -c "import json; print(json.load(open('.secrets.baseline')).get('version',''))")
          echo "baseline version: $VERSION"
          case "$VERSION" in
            *ibm*) ;;
            *)
              echo "blocked=true" >> "$GITHUB_OUTPUT"
              {
                echo "problem<<EOF"
                echo "\`.secrets.baseline\` came from upstream detect-secrets (\`$VERSION\`), not IBM's fork."
                echo ""
                echo "Delete it and regenerate. There is no conversion between the two formats."
                echo "EOF"
              } >> "$GITHUB_OUTPUT"
              echo "::error::Baseline is not from the IBM fork."
              exit 1 ;;
          esac

          # --update rewrites the baseline in place, keeping the exclusions it already records,
          # and adds anything new with no verdict attached. So a secret this pull request adds
          # shows up as an unaudited entry, which the audit below then refuses.
          #
          # No `set -e` in this step: the audit needs its exit code captured, not acted on. So
          # the scan gets checked by hand. A scan failure that slips through leaves the previous
          # baseline in place, that baseline passes, and the pull request merges unscanned.
          if ! detect-secrets scan --update .secrets.baseline; then
            echo "blocked=true" >> "$GITHUB_OUTPUT"
            {
              echo "problem<<EOF"
              echo "The secret scanner could not read this repository."
              echo ""
              echo "A malformed \`.secrets.baseline\` is the usual cause. Regenerating it normally fixes it:"
              echo ""
              echo "    rm .secrets.baseline"
              echo "    detect-secrets scan --update .secrets.baseline"
              echo "    detect-secrets audit .secrets.baseline"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
            echo "::error::detect-secrets scan failed, so nothing was checked. Failing closed."
            exit 1
          fi

          # Three ways to fail: a finding that has not been ruled on, including whatever the scan just
          # added; a secret the scanner confirmed still works; or one somebody marked genuine and
          # left in the code.
          OUTPUT=$(detect-secrets audit --report \
            --fail-on-unaudited --fail-on-live --fail-on-audited-real \
            --omit-instructions .secrets.baseline 2>&1)
          STATUS=$?
          echo "$OUTPUT"

          if [ "$STATUS" -eq 0 ]; then
            echo "blocked=false" >> "$GITHUB_OUTPUT"
            echo "Secret scan passed." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          echo "blocked=true" >> "$GITHUB_OUTPUT"
          {
            echo "problem<<EOF"
            echo "The secret scan found something this pull request has not accounted for."
            echo ""
            echo '```'
            printf '%s\n' "$OUTPUT" | head -40
            echo '```'
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          echo "::error::Secret scan failed."
          exit 1

      - name: Label the pull request
        if: always() && steps.baseline.outputs.present == 'true' && steps.install.outcome == 'success'
        env:
          BLOCKED: ${{ steps.scan.outputs.blocked }}
        run: |
          set -euo pipefail
          # Adding a label the repo lacks creates it. Removing one the pull request does not
          # carry gives a 404. That is fine, not an error.
          add() { gh api -X POST "repos/$REPO/issues/$PR/labels" -f "labels[]=$1" >/dev/null || \
                    echo "::warning::could not add the $1 label"; }
          remove() { gh api -X DELETE "repos/$REPO/issues/$PR/labels/$1" >/dev/null 2>&1 || true; }
          if [ "$BLOCKED" = "true" ]; then add secrets-blocked; else remove secrets-blocked; fi

      - name: Explain the block, or withdraw the explanation
        if: always() && steps.baseline.outputs.present == 'true' && steps.install.outcome == 'success'
        env:
          BLOCKED: ${{ steps.scan.outputs.blocked }}
          PROBLEM: ${{ steps.scan.outputs.problem }}
        run: |
          set -euo pipefail
          existing=$(gh api "repos/$REPO/issues/$PR/comments" --paginate \
            --jq '.[] | select(.user.login == "github-actions[bot]" and (.body | contains(env.MARKER))) | .id' \
            | awk 'NR==1') || existing=""

          if [ "$BLOCKED" != "true" ]; then
            # Nothing wrong any more. Take the comment down.
            if [ -n "$existing" ]; then
              gh api -X DELETE "repos/$REPO/issues/comments/$existing" >/dev/null &&
                echo "Removed the block comment." || echo "::warning::could not remove the block comment"
            fi
            exit 0
          fi

          # printf line by line because a heredoc body has to start at column 0, and column 0
          # ends the YAML block this script lives in.
          fix=$(printf '%s\n' \
            '**If it is a real secret:** take it out of the code, revoke the credential, then re-scan. Do not put it in the baseline.' \
            '' \
            '**If it is a false positive on one line:** add a `pragma: allowlist secret` comment to that line.' \
            '' \
            '**If there are several:** re-scan, record your verdicts, and commit the baseline.' \
            '' \
            '    detect-secrets scan --update .secrets.baseline' \
            '    detect-secrets audit .secrets.baseline' \
            '' \
            'The baseline is an audit record, not an allowlist. Every entry carries a verdict because somebody looked at it, and this check refuses any entry that does not.')
          body=$(printf '%s\n%s\n\n%s\n' "$MARKER" "$PROBLEM" "$fix")

          if [ -n "$existing" ]; then
            gh api -X PATCH "repos/$REPO/issues/comments/$existing" -f body="$body" >/dev/null &&
              echo "Updated the block comment." || echo "::warning::could not update the block comment"
          else
            gh api -X POST "repos/$REPO/issues/$PR/comments" -f body="$body" >/dev/null &&
              echo "Explained the block." || echo "::warning::could not post the block comment"
          fi
